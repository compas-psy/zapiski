<?xml version="1.0" encoding="utf-8"?>
<!--
  Виджет «Записать» 1×1 (BEHAVIOR §8).
  updatePeriodMillis="0" — обновление по изменению данных, а не по таймеру.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_quick_note"
    android:previewLayout="@layout/widget_quick_note"
    android:previewImage="@mipmap/ic_launcher"
    android:description="@string/widget_quick_note_description"
    android:resizeMode="none"
    android:widgetCategory="home_screen" />
