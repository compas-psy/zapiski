/**
 * Тесты оболочки Android.
 *
 * До сих пор у неё не было ни одного теста на TypeScript: `pnpm test` гонял
 * только самопроверку оверлея манифеста. Между тем именно здесь живёт код,
 * который распоряжается указателем на папку с заметками, — и дефект «папка
 * потерялась» пришлось чинить дважды, в двух местах, потому что правило
 * нигде не было записано.
 *
 * Окружение — node: ни одного экрана в оболочке нет и быть не должно
 * (ARCHITECTURE §1), проверяются чистые решения над платформенными портами.
 */
import { defineConfig } from 'vitest/config';

export default defineConfig({
  /* Версию в сборку подставляет `vite.config.ts`. Тестам она не интересна, но
     без подстановки код оболочки не соберётся вовсе: `__ZAPISKI_VERSION__`
     остался бы неизвестным именем. */
  define: { __ZAPISKI_VERSION__: JSON.stringify('0.0.0-test') },
  test: {
    environment: 'node',
    include: ['test/**/*.test.ts'],
  },
});
