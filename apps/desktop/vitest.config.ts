/**
 * Тесты оболочки Windows и macOS.
 *
 * До сих пор у неё не было ни одного теста на TypeScript. Это и есть причина,
 * по которой `kind: 'windows'` пролежал захардкоженным до самой сборки под
 * Мак: поле уезжает серверу при запросе ссылки для входа, а проверить его
 * было негде. Прогон заводится ровно затем, чтобы контракт платформы
 * стерёгся здесь, а не на машине заказчика.
 *
 * Окружение — node: экранов в оболочке нет и быть не должно
 * (ARCHITECTURE §1), проверяются чистые решения над платформенными портами.
 */
import { defineConfig } from 'vitest/config';

export default defineConfig({
  /* Версию в сборку подставляет `vite.config.ts`; без подстановки код
     оболочки не соберётся вовсе — `__ZAPISKI_VERSION__` остался бы
     неизвестным именем. */
  define: { __ZAPISKI_VERSION__: JSON.stringify('0.0.0-test') },
  test: {
    environment: 'node',
    include: ['test/**/*.test.ts'],
  },
});
