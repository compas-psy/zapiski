/**
 * Реестр текстов из BEHAVIOR.md §11.
 *
 * Инвариант ARCHITECTURE.md §3.10: тексты ошибок — дословно из реестра.
 * Ни одна строка здесь не редактируется «по смыслу»: если нужен другой текст,
 * он сначала добавляется в BEHAVIOR.md §11, потом сюда.
 *
 * Клиент всегда получает пару `code` + `message`. `code` — машиночитаемый и
 * стабильный, `message` — то, что можно показать пользователю как есть.
 */

/** Тексты, присутствующие в реестре BEHAVIOR §11 дословно. */
export const REGISTRY = {
  /** Нет сети при синке. */
  offline: 'Оффлайн · всё сохранено локально',
  /** Синк не удался. */
  syncFailed: 'Не удалось синхронизировать · Повторить',
  /** Нет места на диске. */
  diskFull:
    'На устройстве закончилось место. Заметка сохранена в памяти — освободите место, чтобы записать её на диск',
  /** Нет доступа к папке. */
  folderUnavailable: 'Папка недоступна. Возможно, её переместили — укажите новое расположение',
  /** WebDAV: неверные данные. */
  webdavBadCredentials: 'Сервер не принял логин или пароль',
  /** WebDAV: сервер недоступен. */
  webdavUnreachable: 'Сервер не отвечает · Повторить',
  /** Токен Яндекс.Диска истёк. */
  yandexTokenExpired: 'Нужно снова войти в Яндекс.Диск',
  /** Magic-ссылка просрочена. */
  magicLinkExpired: 'Ссылка больше не действует. Прислать новую?',
  /** Неверный пароль шифрования. */
  wrongEncryptionPassword: 'Пароль не подошёл',
  /** Задержка после попыток. */
  tooManyAttempts: 'Попробуйте через 30 секунд',
  /** Файл повреждён. */
  fileCorrupted:
    'Не удалось прочитать файл. Он остался на диске — можно открыть его другим редактором',
  /** Конфликт объединён. */
  conflictMerged: 'Версии объединены · История',
  /** Конфликт шифрованной. */
  conflictEncrypted: 'Заметка менялась на двух устройствах. Обе версии сохранены',
  /** Подписка истекла. */
  subscriptionExpired:
    'Подписка закончилась. Заметки на месте, синхронизация через облако КОМПАС приостановлена',
} as const;

/**
 * «Письмо не дошло» — параметризованный текст реестра:
 * «Письмо ушло на marina@ya.ru. Не пришло — проверьте «Спам» или отправьте снова».
 */
export function mailSent(email: string): string {
  return `Письмо ушло на ${email}. Не пришло — проверьте «Спам» или отправьте снова`;
}

/**
 * Ситуации, которых в реестре §11 пока нет.
 *
 * Формулировки следуют правилам §11 (без восклицательных знаков; что
 * произошло и что можно сделать; не винить пользователя; не употреблять слова
 * «ошибка»/«сбой»/«внимание»). Каждая строка — кандидат на внесение в реестр;
 * список продублирован в server/README.md, раздел «Тексты вне реестра».
 */
export const PENDING_REGISTRY = {
  /** Квота облака исчерпана. */
  quotaExceeded: 'В облаке закончилось место. Удалите ненужные файлы или расширьте тариф',
  /** Файл больше лимита одного блоба. */
  blobTooLarge: 'Файл слишком большой для облака — предел 64 МБ',
  /** Оптимистичная блокировка: etag разошёлся. */
  etagMismatch: 'Файл изменился на другом устройстве. Обновите его и попробуйте снова',
  /** Нужен вход. */
  authRequired: 'Нужно войти в аккаунт КОМПАС',
  /** Сессия отозвана либо истекла. */
  sessionExpired: 'Сессия закончилась. Войдите снова',
  /** Ничего не нашли по такому пути/идентификатору. */
  notFound: 'Ничего не нашли по этому адресу',
  /** Некорректный ввод. */
  badRequest: 'Не удалось разобрать запрос',
  /** Внутренняя неполадка. */
  internal: 'Сейчас не получилось. Попробуйте ещё раз',
  /**
   * Письмо со ссылкой не ушло.
   *
   * Текст называет ПРИЧИНУ и даёт рабочий путь: вход через Яндекс ID рядом и
   * от почтового релея не зависит. Прежде на это место попадал текст про
   * синхронизацию — сообщение не о том, что произошло.
   */
  mailFailed:
    'Письмо со ссылкой отправить не удалось. Войдите через Яндекс ID или попробуйте позже',
  /** Внешний платёжный провайдер не настроен. */
  billingUnavailable: 'Оплата временно недоступна. Попробуйте позже',
  /**
   * Оплата не подключена вовсе (BILLING_ENABLED=false). Это не поломка и не
   * «попробуйте позже»: платить сейчас не за что, всё и так открыто.
   */
  billingDisabled: 'Оплата пока не подключена — всё доступно бесплатно',
  /**
   * Приёмник аналитики не отвечает ни на выключенный флаг ANALYTICS_ENABLED,
   * ни на отсутствующее/отозванное согласие конкретного человека — оба
   * случая клиент трактует одинаково: молча не отправлять и не жаловаться.
   */
  analyticsDisabled: 'Аналитика не включена',
  /**
   * Подписки НЕ БЫЛО. Отдельный текст от «закончилась» именно потому, что
   * закончиться может только начавшееся: человеку, который никогда не платил,
   * сообщение про конец подписки говорит о событии, которого не было.
   */
  subscriptionRequired:
    'Синхронизация через Облако Записок входит в подписку. Заметки на месте и открываются как обычно',
  /** Публикация выключена в конфигурации. */
  publishDisabled: 'Публикация по ссылке пока недоступна',
} as const;

export type RegistryKey = keyof typeof REGISTRY;
export type PendingRegistryKey = keyof typeof PENDING_REGISTRY;
