# Монетизация

Рамка семейства КОМПАС: цена ниже психологического порога, честный бесплатный
тариф.

## Тарифы

| | **Бесплатно** | **ЗАПИСКИ+** |
| --- | --- | --- |
| Цена | 0 | **299 ₽/мес** · **2 688 ₽/год** (224 ₽/мес) |
| Все функции редактора | ✓ | ✓ |
| Шифрование | ✓ | ✓ |
| Экспорт md / PDF / HTML / DOCX | ✓ | ✓ |
| Импорт из Bear, Obsidian, Notion, Evernote | ✓ | ✓ |
| Локальная папка + WebDAV + Яндекс.Диск | ✓ | ✓ |
| Облако Записок (E2E, мгновенный синк) | — | ✓, 10 ГБ |
| История версий | 7 дней локально | до года в облаке (30 дней в пробном) |
| Веб-доступ к заметкам | только своё хранилище | ✓ |
| Публикация по ссылке (P1) | — | ✓ ⏳ |
| Голос: обработка в облаке (P1) | на устройстве бесплатно | ✓ ⏳ |
| **Бандл с приложением практики** | — | +100 ₽ к тарифу практики |

Пробный период — без карты и без таймеров, длительность зависит от даты
подключения облака:

* **30 дней** тем, кто подключил облако строго **до 01.09.2026**;
* **14 дней** всем остальным.

Правило заказчика, источник правды — `packages/core/src/trial.ts`. Интерфейс
обязан показывать тот срок, который человек получит на самом деле: подпись
«Попробовать 14 дней» при фактических 30 днях — обещание меньше сделанного, и
доверие оно портит так же, как обещание больше. Поэтому кнопка берёт число из
`trialDaysFor`, а не из строки перевода.

Льготный период после неудачного продления — 7 дней (`grace`).

Значения в коде: `PRICE_MONTHLY_RUB = 299`, `PRICE_YEARLY_MONTHLY_RUB = 224`,
`TRIAL_DAYS = 14`, `GRACE_DAYS = 7`, `QUOTA_BYTES = 10 ГиБ`,
`VERSION_RETENTION_TRIAL_DAYS = 30`, `VERSION_RETENTION_PAID_DAYS = 365`
(`server/src/config/env.ts`).

## Логика цены

299 ₽ — «чашка кофе», импульсная цена для массового рынка. Заметочник — B2C, а
не B2B: ценовая рамка «стоимость сессии», работающая для профессиональных
инструментов, здесь не применима.

Ориентиры юнит-экономики из концепции:

| Показатель | Ориентир |
| --- | --- |
| Конверсия free → paid | 4–7% (норма для freemium-заметочников) |
| ARPU платящего | ~270 ₽/мес |
| CAC через сообщество | 300–600 ₽ |
| Payback | 3–5 месяцев |

Бандл с Дневником усиливает retention основного продукта — это его главная
задача, а не дополнительная выручка.

## За что именно платит пользователь

За **облако и серверы**, а не за снятие искусственных ограничений. Это
формулировка с самого экрана paywall: «Платите за облако и серверы. Всё
остальное — бесплатно навсегда».

Практическое следствие: если пользователю хватает Яндекс.Диска или своего
WebDAV — он никогда не увидит повода платить, и это нормально. Бесплатный тариф
не демо-версия, он самодостаточен.

## Правила paywall

Экран — одна страница шириной 600 (`SCREENS.md` §9): вордмарк «ЗАПИСКИ+»,
честный подзаголовок, цена, таблица сравнения, кнопка «Попробовать» со сроком
из `trial.ts` (сейчас 30 дней, с 01.09.2026 — 14),
внизу — «Отмена в один тап. Без таймеров и „скидок только сегодня“» и бандл.

### Запрещено — пункт F приёмочного листа

| Запрет | Почему |
| --- | --- |
| **Таймеры обратного отсчёта** | Искусственная срочность противоречит позиционированию «спокойный продукт» |
| **Предвыбранный годовой тариф без явной пометки** | Тёмный паттерн: человек платит за год, думая, что платит за месяц |
| **Скрытая кнопка закрытия** | Экран, из которого нельзя выйти, — это вымогательство, а не продажа |
| **Блокировка доступа к уже созданным заметкам** | Прямое нарушение принципа «file over app»: заметки лежат на диске пользователя, и держать их в заложниках мы не имеем права |

### Что происходит при истечении подписки

Правило, зашитое в сервер и покрытое тестом
(`server/test/billing.entitlement.test.ts`):

* **чтение доступно всегда** — ни один `GET` не спрашивает про подписку;
* **удаление разрешено всегда** — иначе пользователь, упёршийся в квоту,
  оказался бы заперт без выхода;
* запрещается только **запись новых данных** в облако: `402
  subscription_expired`;
* локальные заметки не затрагиваются вообще — они и так на диске;
* текст сообщения дословно из реестра `BEHAVIOR.md` §11: «Подписка
  закончилась. Заметки на месте, синхронизация через Облако Записок
  приостановлена».

## Платёжные провайдеры

**Решение учредителя от 18.08.2026: Т-Касса — единственный эквайринг портфеля.**
Один провайдер на все продукты СИМПАС.

| Провайдер | Где | Состояние |
| --- | --- | --- |
| **Т-Касса (Т-Банк)** | Веб, десктоп, Android | ✅ на сервере: `Init` с чеком 54-ФЗ, уведомления с подписью в теле, идемпотентность по `(provider, event_id)`, льготный период |

Других нет и в коде тоже: `yookassa.ts`, `googlePlay.ts`, их маршруты,
переменные окружения и тесты удалены. Не отключены флагом — **удалены**:
платёжный путь, который нельзя пройти, но который выглядит рабочим, опаснее
отсутствующего.

Что при этом осталось нетронутым — **история**. Платежи ЮKassa и Google Play
лежат в базе, включая непроведённые, и читаются как прежде: тип
`PaymentProvider` допускает все три значения, CHECK-ограничения не сужены.
Правило простое: **читаем все три, пишем только Т-Кассу** (`ActiveProvider`).
Стережёт это `server/test/billing.history.test.ts` — на строке с
`provider='yookassa'` и на самом тексте ограничений в схеме.

Почему провайдеры отпали:

* **Google Play — не по нашему выбору.** С декабря 2024 Google не платит
  российским разработчикам. Серверная валидация покупок была бы кодом, который
  никогда не выполнится.
* **Два эквайринга на два продукта — это две сверки.** ПРАКТИКА уже ходит в
  Т-Банк; держать рядом ЮKassa значит удваивать интеграцию, отчётность и число
  мест, где ломается.

Устройство подписи Т-Кассы отличается от прошлого провайдера, и об этом легко
забыть: **заголовка подписи нет**. Подпись — поле `Token` внутри тела: значения
корневых параметров сортируются по имени ключа, склеиваются с паролем терминала
и хешируются SHA-256; `Token`, `Receipt` и `DATA` в подпись не входят.
Следствие: тело разбирается до проверки, и доверять ему до `verifyNotification`
нельзя.

Второе следствие, продуктовое: **в уведомлении Т-Кассы нет ни идентификатора
человека, ни тарифа**. Связь «кто и за что платит» пишется у нас
(`payment_orders`) до перехода на оплату и читается по номеру заказа после.
Свои идентификаторы на чужую сторону ради удобства разбора не отправляем: и
`OrderId`, и `CustomerKey` — непрозрачные значения.

**Автосписания нет.** Первый платёж уходит с `Recurrent: 'Y'` и `CustomerKey`,
банк присылает `RebillId`, мы его сохраняем — но регулярное списание требует
планировщика, которого в MVP нет. До его появления подписка продлевается
повторной оплатой, и интерфейс обязан говорить это прямо. `RebillId` —
платёжный секрет: он не попадает ни в аналитику, ни в панель, ни в журнал
событий.

Восстановление покупок и grace period реализованы в
`server/src/services/subscription.ts`.

> ⏳ **Заплатить всё равно нельзя.** Экраны есть: paywall (проверяется тестом
> на отсутствие тёмных паттернов), экран входа и раздел «ЗАПИСКИ+» в настройках.
> Но цепочка обрывается раньше оплаты: приложение не умеет завершить вход —
> письмо magic-link уходит, страница Яндекс ID открывается, а приём токена в
> оболочках не написан. Без аккаунта нет ни подписки, ни облака.
>
> Плюс отдельно: клиент Облака Записок не сходится с сервером по путям
> эндпоинтов, поэтому даже с аккаунтом главная платная функция не заработала бы.
> Разбор — [`../dev/modules/core.md`](../dev/modules/core.md#протокол-облака).
>
> Серверная сторона при этом готова целиком: статус подписки, пробный период,
> отмена, платежи и вебхуки.

## Аккаунт

Аккаунт нужен **только для Облака Записок** и для веб-доступа/публикации.
Локальный режим работает без аккаунта вообще: онбординг заканчивается курсором
в первой заметке, регистрации нет.

Способы входа:

1. **Яндекс ID (OAuth)** — основной путь, бесплатен для нас;
2. **Magic-link на email** — письмо со ссылкой, вход в один тап. Токен
   одноразовый, TTL 15 минут, привязан к устройству инициации;
3. Telegram Login Widget — опционально, P1.

**Паролей нет.** Нет экрана «придумайте пароль» и нет «забыли пароль» — пароль
существует только для шифрования заметок, и это другой, локальный контур.

**SMS не используется нигде** — ни эндпоинта, ни поля, ни зависимости. Это
прямой запрет ТЗ §5.5 и проверяемый пункт приёмки A6.

SSO с приложением практики — общий аккаунт, но вход в ЗАПИСКИ **никогда не требует
Дневника**.
