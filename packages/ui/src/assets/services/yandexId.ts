/**
 * Логотип Яндекс ID для кнопки входа.
 *
 * ── Что было сломано ────────────────────────────────────────────────────────
 *
 * Кнопка ссылалась на `/assets/yandex-logo.png` — адрес, по которому файла нет
 * ни в одной сборке. Оригинал лежит в `docs/spec/assets/`, то есть в
 * документации: в веб-сборку он не попадал, в apk и установщик тем более.
 * Результат одинаков везде: на кнопке пусто.
 *
 * ── Почему теперь так ───────────────────────────────────────────────────────
 *
 * Файл лежит внутри пакета и берётся через `new URL(…, import.meta.url)` —
 * тем же способом, что знак сервиса рядом. Сборщик переписывает это в адрес
 * эмитированного ассета, поэтому путь верен во всех трёх оболочках и не
 * зависит от того, что и куда скопировали руками. Абсолютный путь от корня
 * («/assets/…») такой гарантии не даёт: у Tauri корень свой.
 *
 * Картинка — официальный знак, уменьшенный до 64×64 (кнопке нужно 20 px, с
 * запасом на плотные экраны). Не перерисован: логотип чужой, и рисовать его
 * похожим нельзя.
 */
export const YANDEX_ID_LOGO = new URL('./yandex-id.png', import.meta.url).href;
