/**
 * Зеркало типов из `packages/core/src/contract.ts`, нужных редактору.
 *
 * Почему копия, а не импорт: `@zapiski/core` пишется параллельно и ещё не
 * собран (`dist/` пуст), а глубокие импорты в чужой `src/` запрещены
 * (ARCHITECTURE §5). Типы структурные — как только ядро соберётся, достаточно
 * заменить тело файла на `export type { … } from '@zapiski/core';`, ни одна
 * строка остального кода не изменится.
 */

/** Сила тактильной подсказки (BEHAVIOR §0). */
export type HapticStrength = 'light' | 'medium';

/** Путь внутри vault'а, всегда с прямыми слэшами и без ведущего слэша. */
export type VaultPath = string;
