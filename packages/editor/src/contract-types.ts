/**
 * Типы ядра, нужные редактору.
 *
 * Здесь была копия объявлений: пока `@zapiski/core` писался параллельно и его
 * `dist/` был пуст, настоящий импорт делал `tsc --noEmit` красным. Ядро
 * собрано — копия убрана, осталась одна точка правды. Реэкспорт сохранён,
 * чтобы остальной код редактора не менялся.
 */

export type { HapticStrength, VaultPath } from '@zapiski/core';
