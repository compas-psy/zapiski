/**
 * Документы, на которые человек даёт согласие при регистрации.
 *
 * ── Почему это одно место на весь продукт ───────────────────────────────────
 *
 * Согласие даётся на КОНКРЕТНУЮ редакцию текста, и редакция уезжает на сервер
 * вместе с согласием. Если приложение показывает одну редакцию, а отправляет
 * другую, собранное согласие ничего не подтверждает. Поэтому строка ровно
 * одна, и берут её отсюда все три оболочки.
 *
 * Дата в формате `ГГГГ-ММ-ДД` — это версия, а не «когда собрали». Меняете
 * текст документов — меняйте и её: прежнее согласие не является согласием на
 * новый текст, и приложение обязано спросить снова.
 */
export const LEGAL_VERSION = '2026-08-13';

/**
 * Где документы лежат для чтения.
 *
 * Адреса на нашем домене, а не файлы в приложении: документы правятся без
 * выпуска новой версии приложения, и человек, у которого стоит старая сборка,
 * обязан читать действующую редакцию.
 */
export const LEGAL_URLS = {
  terms: 'https://zapiski.cmpas.ru/terms',
  privacy: 'https://zapiski.cmpas.ru/privacy',
} as const;
