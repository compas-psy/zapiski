/**
 * Юридические документы: редакция и адреса.
 *
 * ── Почему это одно место на весь продукт ───────────────────────────────────
 *
 * Соглашение принимается ДЕЙСТВИЕМ, и вместе с действием на сервер уезжает
 * редакция того текста, который человеку показали. Если приложение показывает
 * одну редакцию, а отправляет другую, записанное согласие не подтверждает
 * ничего. Поэтому строка ровно одна, и берут её отсюда все три оболочки.
 *
 * ── Что означает версия ─────────────────────────────────────────────────────
 *
 * Это номер редакции ПАКЕТА документов CMPAS, а не дата сборки приложения.
 * Пакет v0.9 от 14 августа 2026 года. Меняется пакет — меняется и здесь, и
 * прежнее согласие перестаёт быть согласием на новый текст: приложение обязано
 * спросить снова (CMPAS Legal Implementation §12).
 */
export const LEGAL_VERSION = '0.9';

/**
 * Где документы лежат для чтения.
 *
 * Адреса на нашем домене, а не файлы в приложении: документы правятся без
 * выпуска новой версии приложения, и человек, у которого стоит старая сборка,
 * обязан читать действующую редакцию.
 *
 * Пути — из таблицы §2 инструкции пакета. Прежние `/terms` и `/privacy`
 * сервер продолжает отдавать: их знают уже выложенные сборки, и ломать ссылку,
 * по которой человек читает то, что принимает, нельзя.
 */
export const LEGAL_URLS = {
  /** Центральное соглашение Экосистемы: принимается действием. */
  terms: 'https://zapiski.cmpas.ru/legal/terms',
  /** Политика ПДн: информационный документ, не принимается никогда. */
  privacy: 'https://zapiski.cmpas.ru/legal/privacy',
  /** Особые условия ЗАПИСОК: принимаются при первом подключении сервиса. */
  notes: 'https://zapiski.cmpas.ru/legal/notes',
  /** Текст рекламного согласия — того самого, что даётся галочкой. */
  marketing: 'https://zapiski.cmpas.ru/legal/consent/marketing',
  /** Реестр сервисов Экосистемы. */
  services: 'https://zapiski.cmpas.ru/legal/services',
} as const;
