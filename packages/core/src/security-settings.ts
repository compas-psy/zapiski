/**
 * Показывать ли раздел «Безопасность» в настройках.
 *
 * ── Решение заказчика ───────────────────────────────────────────────────────
 *
 * «Раздел Безопасность сделай неактивным и подписью СКОРО».
 *
 * Это продолжение более раннего решения о MVP: «шифрование — тяжёлая функция,
 * давай её из MVP вырежем; продумаем позже и сделаем с современной биометрией
 * и/или Яндекс Ключом». Раздел собран вокруг пароля хранилища, и до этого
 * разговора трогать в нём нечего.
 *
 * ── Почему пункт остаётся видимым ───────────────────────────────────────────
 *
 * Правило «отсутствующая возможность скрыта, а не выключена» (BEHAVIOR §5.1)
 * говорит о возможностях, которых НЕ БУДЕТ. Здесь обратный случай: раздел
 * вернётся, и «СКОРО» — это обещание, а не серая кнопка-обманка. Убрать пункт
 * совсем значило бы, что человек, однажды его видевший, решит, будто
 * настройки урезали молча.
 *
 * ── Что выключатель НЕ делает ───────────────────────────────────────────────
 *
 * Он не трогает само шифрование: заметка по-прежнему шифруется из меню
 * заметки, пароль по-прежнему спрашивается на разблокировке, автозамок
 * работает на своём умолчании. Спрятаны только НАСТРОЙКИ этого.
 */
export const SECURITY_SETTINGS_ENABLED = false;
